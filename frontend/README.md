# Dental Task Manager – Frontend (React + Vite + Tailwind)

## Quick start
```bash
cd frontend
npm install
echo "VITE_API_BASE=http://localhost:8000" > .env
npm run dev
```
Login with any of the demo accounts listed in the backend README.